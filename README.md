Add a default note in picking types
-----------------------------------
This module allow to specify a note in picking type so that it can be used as default one when creating picking of that type.



